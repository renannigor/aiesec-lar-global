import 'package:flutter/material.dart';

class MinhasHospedagens extends StatelessWidget {
  const MinhasHospedagens({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}